stages:
    - build
    - test
    - deploy

build job:
    stage: build 
    script:
        - echo "This is a first Demo build job"

test job:
    stage: test
    script:
        - echo "This is a first Demo test job"

deploy job:
    stage: deploy
    script:
        - echo "This is a first Demo deploy job"
