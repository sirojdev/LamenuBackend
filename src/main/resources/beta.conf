ktor {
  deployment {
    port = 9001
    host = "0.0.0.0"
  }

  application {
    modules = [mimsoft.io.Application.kt.modeule]
  }
  dataSource {
    url = "jdbc:postgresql://lamenu.uz:5432/lamenu_beta"
    username = "postgres"
    password = "re_mim_soft"
    hikari {
      maximum-pool-size = 10
    }
    connection-timeout = 5000
    validate-on-acquire = true
  }
}